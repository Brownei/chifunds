export type User = {
  user: {
    id: number,
    email: string,
    first_name: string,
    last_name: string,
    profile_picture: string,
    email_verified: boolean,
    account_number: number,
    balance: number,
  }
}
